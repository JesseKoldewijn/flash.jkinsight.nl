---
import MetaTags from "./injects/meta.astro";
import TailwindInject from "./injects/tailwind.astro";
import ThemeSwitch from "./injects/theme.astro";
import Navbar from "./navbar/navbar.astro";
const { title } = Astro.props;
---

<html lang="en">
  <head>
    <title>{`${title ? title + " | " : ""}JKinsight Flash 🚀`}</title>
    <meta
      name="description"
      content={`${title ? title + " | " : ""}JKinsight Flash 🚀`}
    />
    <MetaTags />
    <TailwindInject />
    <ThemeSwitch />
  </head>
  <body>
    <main>
      <Navbar />
      <slot />
    </main>
  </body>
</html>
